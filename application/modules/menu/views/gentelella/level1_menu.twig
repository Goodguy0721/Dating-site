{% for item in menu %}
    <li{% if item.active == 1 %} class="active"{% endif %}>
        <a id="{{ item.gid }}" href="{{ item.link }}">{{ item.value }}
            {% if item.indicator %}<span class="num">{{ item.indicator }}</span>{% endif %}
        </a>
    </li>
{% endfor %}
