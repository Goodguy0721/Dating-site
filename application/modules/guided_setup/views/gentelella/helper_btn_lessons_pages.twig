{% if guided_menu %}
    <div class="row" style="border-bottom:2px solid #e6e9ed">
        <div class="guide_link" id="btn-lessons_page">
            <i class="fa fa-sliders pr5" aria-hidden="true"></i>
            {% helper lang.l('admin_btn_lessons_pages', 'guided_setup') %}
        </div>
    </div>

    <script type="text/javascript">
        $(function(){
                loadScripts(
                        [
                            "{% helper utils.jscript('guided_setup', 'frame-slimscroll.js', 'path') %}",
                            "{% helper utils.jscript('guided_setup', 'guided_setup.js', 'path') %}",
                        ],
                        function(){
                                lessons_pages = new GuidedSetup({
                                        siteUrl: site_url,
                                        guidedMenuId: {{ guided_menu.id }},
                                        frameName: '{{ guided_menu.gid }}',
                                        btnGuidePage: '#btn-lessons_page',
                                        useFrame: 0,
                                });
                        },
                        ['lessons_pages'],
                        {async: false}
                );

        });
    </script>

{% endif %}