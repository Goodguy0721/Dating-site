{% include '@app/header.twig' %}

<script>
	function get_drag_data(point_gid, lat, lon) {
		$('#default_lat').val(lat);
		$('#default_lon').val(lon);
	}
	function test1(t) {
		console.log(t);
	}
	function test2(t) {
		console.log(t);
	}
	function test3(t, t1, t2) {
		console.log(t);
		console.log(t1);
		console.log(t2);

	}
</script>

<div class="lc">
	{% helper content.get_content_tree(page.id) %}
</div>
<div class="rc">
	<div class="edit-block">
		<h1>
            {% helper lang.l('map_example_header', 'geomap') %}
        </h1>
		{% helper geomap.show_default_map({
            'map_gid': map_gid,
            'id_user': user_id,
            'markers': markers,
            'settings': map_settings
        }) %}
	</div>
</div>
<div class="clr"></div>

{% include '@app/footer.twig' %}
